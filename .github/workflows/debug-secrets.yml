name: Debug Secrets

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - name: Check Secrets
      run: |
        echo "Checking if secrets exist..."
        echo "FRONTEND_URL: ${{ secrets.FRONTEND_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "CORS_ORIGIN: ${{ secrets.CORS_ORIGIN != '' && 'EXISTS' || 'MISSING' }}"
        echo "SUPABASE_URL: ${{ secrets.SUPABASE_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY != '' && 'EXISTS' || 'MISSING' }}"
        echo "SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' && 'EXISTS' || 'MISSING' }}"
        echo "DATABASE_URL: ${{ secrets.DATABASE_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_MERCHANT_ID: ${{ secrets.MIDTRANS_MERCHANT_ID != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_CLIENT_KEY: ${{ secrets.MIDTRANS_CLIENT_KEY != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_SERVER_KEY: ${{ secrets.MIDTRANS_SERVER_KEY != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_IS_PRODUCTION: ${{ secrets.MIDTRANS_IS_PRODUCTION != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_3DS: ${{ secrets.MIDTRANS_3DS != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_SANITIZED: ${{ secrets.MIDTRANS_SANITIZED != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_PAYMENT_NOTIFICATION_URL: ${{ secrets.MIDTRANS_PAYMENT_NOTIFICATION_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_RECURRING_NOTIFICATION_URL: ${{ secrets.MIDTRANS_RECURRING_NOTIFICATION_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_PAY_ACCOUNT_NOTIFICATION_URL: ${{ secrets.MIDTRANS_PAY_ACCOUNT_NOTIFICATION_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_FINISH_REDIRECT_URL: ${{ secrets.MIDTRANS_FINISH_REDIRECT_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_UNFINISH_REDIRECT_URL: ${{ secrets.MIDTRANS_UNFINISH_REDIRECT_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "MIDTRANS_ERROR_REDIRECT_URL: ${{ secrets.MIDTRANS_ERROR_REDIRECT_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "PAYMENT_TIMEOUT_MINUTES: ${{ secrets.PAYMENT_TIMEOUT_MINUTES != '' && 'EXISTS' || 'MISSING' }}"
        echo "AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING != '' && 'EXISTS' || 'MISSING' }}"
        echo "AZURE_STORAGE_CONTAINER_NAME: ${{ secrets.AZURE_STORAGE_CONTAINER_NAME != '' && 'EXISTS' || 'MISSING' }}"
        echo "TICMI_API_KEY: ${{ secrets.TICMI_API_KEY != '' && 'EXISTS' || 'MISSING' }}"
        echo "TICMI_API_URL: ${{ secrets.TICMI_API_URL != '' && 'EXISTS' || 'MISSING' }}"
        echo "LOG_LEVEL: ${{ secrets.LOG_LEVEL != '' && 'EXISTS' || 'MISSING' }}"
